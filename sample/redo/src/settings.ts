export const diffServerURL = 'https://serve.replicache.dev/pull';
export const diffServerAuth = '1';

const dataLayerBase = 'https://replicache-sample-todo.now.sh/serve/';
export const loginURL = `${dataLayerBase}login`;
export const batchURL = `${dataLayerBase}replicache-batch`;
export const clientViewURL = `${dataLayerBase}replicache-client-view`;
