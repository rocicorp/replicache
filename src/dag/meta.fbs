namespace meta;

// Metadata about a chunk, stored separately to enable exploring
// dag without reading entire dag.
table Meta {
    // References from this chunk to other chunks.
    refs: [string];
}

root_type Meta;
